#!/usr/bin/env sh

# --- Wrapper script for Gradle ---
# (Simplified version to bootstrap)
# You should normally run 'gradle wrapper' to generate a full version.

echo "Please run 'gradle wrapper' from a machine with Gradle installed to generate the full wrapper."
echo "Or use Docker to build your services."
